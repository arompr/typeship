/** @publish */
export enum UserRole {
  Admin = 'admin',
  User = 'user',
  Guest = 'guest',
}

/** @publish */
export enum OrderStatus {
  Pending = 'pending',
  Shipped = 'shipped',
  Delivered = 'delivered',
}
