// This file was auto-generated by typeship. Do not edit manually.

export interface Address {
  /** Street name and number. */
  street: string;
  /** City or town. */
  city: string;
  /** ISO 3166-1 alpha-2 country code, e.g. "US" or "DE". */
  country: string;
  /** Postal or ZIP code. */
  postalCode: string;
}

export declare class SyncPlayerCommandPayload {
  token: string;
}

export declare class SyncPlayerCommand {
  public readonly payload: SyncPlayerCommandPayload;
  readonly type: "SYNC_PLAYER";
}

export declare class CreateUserDto {
  name: string;
  email: string;
  role: UserRole;
  address: Address;
  getDisplayName(): string;
  isAdmin(): boolean;
}

export enum UserRole {
  Admin = 'admin',
  User = 'user',
  Guest = 'guest',
}

export enum OrderStatus {
  Pending = 'pending',
  Shipped = 'shipped',
  Delivered = 'delivered',
}

export interface PaginatedResult<T> {
  /** Page of results. */
  items: T[];
  /** Total number of items across all pages. */
  total: number;
  /** Current page number (1-based). */
  page: number;
  /** Number of items per page. */
  pageSize: number;
}

export interface UserListResponse {
  /** Paginated user results. */
  data: PaginatedResult<UserDto>;
  /** Opaque request correlation ID. */
  requestId: string;
}

export type UserId = string;

export type UserEmail = string;

export interface UserDto {
  /** Unique user identifier. */
  id: UserId;
  /** Verified email address. */
  email: UserEmail;
  /** Access role assigned to this user. */
  role: UserRole;
  /** Primary mailing address. */
  address: Address;
  /** Timestamp of account creation (UTC). */
  createdAt: Date;
}
